%h1 Manage membership for #{@user.name} (#{@user.email_address})

- if @user.is_moderator
  .mod-badge
    %p Moderator

= render partial: 'user_form', locals: { user: @user, submit_url: update_user_path, admin: false }
