= render partial: 'admin_toolbar'

%br

%p
  = link_to 'Back', controller: 'subscription_lists', action: 'index'

%h1 Manage list: #{@list.name}

= render partial: 'list_form', locals: { list: @list, submit_url: update_list_path, edit: true }
