= render partial: 'admin_toolbar'

%p
  = link_to 'Back', { controller: 'addresses', action: 'index' }

%h1 Edit address

= render partial: 'address_form', locals: { address: @address, submit_url: update_address_path }
