!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title #{ENV.fetch('MAILING_LIST_NAME')}
    = stylesheet_link_tag    'application', media: 'all'
    = csrf_meta_tags
  %body
    - if flash[:success]
      .flash.success
        = flash[:success]
    = yield
